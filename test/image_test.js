const expect = require('chai').expect;
const bmt = require(__dirname + '../../lib/change.bmp');

// describe('size of the file', ()=>{
//   beforeEach(()=>{
//     // this.file = __dirname + '../palette.bmp'
//   })
// })


describe('size of the file',()=>{
  beforeEach(()=>{
    this.file = __dirame bmp
  })
  it('should equal the size of the newly created file');
})
